[X] Ensure balance of expenses and sinking funds increase by remaining allocation after the month is over as we move to the next month (also allow the balance to go into the negative). - this can be done by the shift to transactions for sinking fund allocation 
[X] Actually, let the allocated dollar amount in a sinking fund be a transaction applied to that sinking fund therefore affecting the balance, and let them be connected so editing the allocated dollar amount will edit the transaction editing the transaction would edit the allocation amount, and the same goes for deleting the transaction or sinking fund.
[X] Sync the month selector between all pages.
[X] When I click on a transaction in the pie chart, take me to the transaction on the transaction page.
[X] When clicking on a transaction under it's Category, let me edit the transaction
[X] Let editing transaction in the Category viewer edit or delete that category's Allocation in Transactions - there is a bug where editing an allocation's transaction, then changing that cateogry's allocation, keeps both the original edited one and the new allocation causing 2 allocation transactions to be applied.
[X] The category legend under the distribution overview gets cutoff because the panel is too short, can you make sure the categories don't get cut off even when I have many-many categories listed under the pie chart.
[X] Include Outflow transactions from Savings/Sinking Funds in the Distribution Overview (but make sure it can be filtered out) 
[X] Make Distribution Overview extremely easy to switch between normal spending, and Sinking fund spending. By default, present normal spending only.
[X] Fix filtering certain categories on Distribution Overview (currently we get some JS errors when trying to filter categories in or out)
[X] Category filtering still doesn't work when clicking on them individually
[X] Make Sinking fund spending fall under the "Normal Spending" dropdown filter; I only want "(Saved)" transactions listed under the  "Sinking Funds" drowndown filter.
[X] Remove the Toggle All button on the Distribution Overview page
[X] Fix "txn linked" and "no txn yet" text
[X] Change "No spending or savings found for this month." when no transactions are available for the pie chart
[X] Fix the CSV Template under Import, it doesn't match the same format as when I export and import a CSV.
